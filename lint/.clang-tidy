Checks: >
  -*,
  bugprone-*,
  performance-*,
  modernize-use-override,
  modernize-use-nullptr,
  modernize-use-auto,
  readability-redundant-declaration,
  readability-misleading-indentation,
  -bugprone-easily-swappable-parameters,
  -bugprone-narrowing-conversions,

# bugprone-*                        Catches real bugs: use-after-move, dangling handles, etc.
# performance-*                     Unnecessary copies, inefficient containers, move semantics
# modernize-use-override/nullptr    Low noise, prevents real mistakes
# modernize-use-auto                Reduces verbose type declarations
# readability-redundant-*           Dead declarations
# readability-misleading-indent     Catches indentation bugs
#
# Disabled:
# bugprone-easily-swappable-params  Too noisy for math-heavy code (float x, float y, float z)
# bugprone-narrowing-conversions    Noisy with CUDA (int/float/size_t conversions everywhere)

HeaderFilterRegex: '(include|cuda|src)/.*\.(h|hpp|cuh)$'

WarningsAsErrors: ''
