set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-ghostty:RGB"
set -as terminal-features ",xterm-256color:RGB"
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
set -s escape-time 0

set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on
set -g detach-on-destroy off

unbind C-b
set-option -g prefix C-s
set-option -g prefix2 none

unbind r
bind r source-file ~/.config/tmux/tmux.conf \; display-message "config reloaded"

unbind %
bind % split-window -h -c "#{pane_current_path}"

unbind '"'
bind '"' split-window -v -c "#{pane_current_path}"

unbind v
bind v copy-mode

unbind !
bind ! break-pane -a

# popup terminal (replaces default clock binding)
unbind t
bind t display-popup -E -w 60% -h 60% -d "#{pane_current_path}"

# popup python REPL: prefer uv (with ipython, picks up project venv); fall back to python3
bind p display-popup -E -w 60% -h 60% -d "#{pane_current_path}" \
  "command -v uv >/dev/null && uv run --with ipython ipython || python3"

# maximise
bind -r m resize-pane -Z

# mouse support
set -g mouse on

# send focus events to apps (vim/neovim)
set -g focus-events on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection
if-shell "command -v xclip >/dev/null 2>&1" \
  "bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel 'xclip -in -selection clipboard'" \
  "bind-key -T copy-mode-vi 'y' send -X copy-pipe-and-cancel 'pbcopy'"

# cyberdream theme
source-file ~/.config/tmux/themes/cyberdream.conf

# pane borders
set -g pane-border-style 'fg=#{@thm_subtext_1}'
set -g pane-active-border-style 'fg=#{@thm_subtext_1}'

# status bar
source-file ~/.config/tmux/status.conf

# window navigation
bind -r n next-window
bind -r p previous-window

set-environment -g TMUX_PLUGIN_MANAGER_PATH ~/.config/tmux/.tmux/plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# auto-install tpm, clone if missing
run-shell 'if [ ! -d $TMUX_PLUGIN_MANAGER_PATH/tpm ]; then git clone https://github.com/tmux-plugins/tpm $TMUX_PLUGIN_MANAGER_PATH/tpm; fi'

run '$TMUX_PLUGIN_MANAGER_PATH/tpm/tpm'
